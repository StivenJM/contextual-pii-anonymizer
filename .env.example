# Copy this file to .env and replace the password with a local-only value.
POSTGRES_DB=contextual_pii
POSTGRES_USER=contextual_pii
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
